
describe('selectionSort', function(){
    it('All numbers should be sorted from least to greatest', () => {2

        let list = [2, 4, 1, 3];
        let expected = [1, 2, 3, 4;
        let result = selectionSort()

        expect(result).toEqual(expected);
    });
});